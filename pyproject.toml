[project]
name = "internship-project"
version = "0.1.0"
description = ""
authors = [
    {name = "Mad"}
]
requires-python = ">3.12,<4.0"
dependencies = [
    "pandas (>=3.0.5,<4.0.0)",
    "numpy (>=2.5.2,<3.0.0)",
    "pyarrow (>=25.0.1,<26.0.0)",
    "rapidfuzz (>=3.14.6,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "jupyter (>=1.1.1,<2.0.0)",
    "ipykernel (>=7.3.0,<8.0.0)"
]
